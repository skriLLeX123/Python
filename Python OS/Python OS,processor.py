from platform import *
print(platform(0,1)) # returns the name of OS running python
print(machine()) #returns the generic name of processor
print(system()) #returns the generic name of OS running python
print(version()) #returns the versio of OS
print(processor()) #returns the full flexed name of the processor
